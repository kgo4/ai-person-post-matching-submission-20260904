[OUTPUT CONTRACT - MANDATORY]
1. Respond with a single valid JSON value only.
2. Do NOT wrap JSON in markdown code fences (no ```json ... ```).
3. Do NOT add explanations, prefixes, or trailing prose outside the JSON.
4. Use double quotes for all keys and string values; no trailing commas.
5. Field names and types must follow the schema exactly; omit unknown fields.
6. If nothing to report, respond with an empty collection matching the schema ({} or []), never free text.
